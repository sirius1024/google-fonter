{
  "name": "google-fonter",
  "version": "0.1.0",
  "main": "main.js",
  "dependencies": {
    "request": "^2.81.0",
    "request-promise": "^4.2.1"
  },
  "devDependencies": {
    "electron-packager": "^8.7.0"
  },
  "scripts": {
    "start": "electron .",
    "package": "electron-packager ./ Google-Fonter --all --out ../build --version 1.6.10 --overwrite --icon=./assets/fonter.ico"
  }
}